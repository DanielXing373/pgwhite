# pgWhite

Nuxt 4 + pnpm。多维检索好词好句（中英）。

## 开发

```
pnpm install
pnpm dev
```

检索首页 `/` 需要 `.env` 里的 Railway `DB_*`。  
内部标注原型（无入口）：`/tagtest`，说明见 `docs/tagtest.md`。

## 构建

```
pnpm build
pnpm preview
```

