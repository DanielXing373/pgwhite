# 内容边框逻辑

**日期**: 2025-12-06  
**提交**: `40db867`

## 开发需求
筛选内容面板的边框颜色需要根据当前激活的标签页动态变化，实现视觉上的无缝连接效果。

## 实现原理
通过计算属性 `contentPanelBorderColor` 根据 `activeTab` 从 `TAB_COLORS` 映射表中获取对应的主题颜色，并动态绑定到内容面板的 `border` 样式。

## 最终效果
当用户切换标签页时，内容面板的边框颜色自动匹配当前激活标签的主题色，形成视觉上的无缝连接。

