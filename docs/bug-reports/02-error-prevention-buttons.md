# 按钮错误预防（禁用状态）

**日期**: 2025-11-05  
**提交**: `e67720d`

## 问题
撤销/重做/清除全部等操作按钮在无可用操作时仍然可点击，导致用户困惑和潜在错误。

## 原理
缺少对操作可用性的状态检查，按钮始终处于可交互状态，无法向用户传达当前操作是否可用。

## 解决方法
为按钮添加 `:disabled` 属性和 `selected-action-btn--disabled` 样式类，当 `canUndo`/`canRedo` 等状态为 `false` 时，按钮显示为灰色并禁用点击，提供清晰的视觉反馈。

