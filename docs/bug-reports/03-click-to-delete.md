# 已选标签点击删除功能

**日期**: 2025-11-05  
**提交**: `c151cb9`

## 开发需求
用户需要能够通过点击已选标签栏中的chip来快速删除筛选条件，提升操作效率。

## 实现原理
在 `SelectedBar` 组件中为每个已选标签chip添加点击事件处理，调用 `@removeTag` 事件并传递对应的 `dimension` 和 `id`，由父组件更新筛选状态。

## 最终效果
用户可以直接点击已选标签栏中的任意chip来移除该筛选条件，无需通过其他操作入口，交互更加直观便捷。

