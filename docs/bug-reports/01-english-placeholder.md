# 英文占位符修复

**日期**: 2025-11-04  
**提交**: `02c5866`

## 问题
界面中的英文占位符文本显示不正确，硬编码的中文文本未使用国际化系统。

## 原理
组件中直接使用了硬编码的中文字符串（如 `'作者'`），未通过 `$t()` 函数调用国际化资源文件。

## 解决方法
将所有硬编码的文本替换为 `$t('filters.authors')` 等国际化键值，并完善 `en.json` 和 `zh.json` 语言包，确保中英文界面正确显示。

