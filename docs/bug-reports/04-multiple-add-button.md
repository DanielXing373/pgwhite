# 多个添加按钮 Bug

**日期**: 2025-12-06  
**提交**: `f536e51`

## 问题
在搜索结果中点击标签chip时，会同时显示多个添加/删除按钮，导致界面混乱。

## 原理
缺少对当前激活chip的唯一性追踪机制，导致多个chip同时显示操作按钮。

## 解决方法
引入 `activeChipId` 状态（包含 `quoteId` 和 `tagId` 的复合键），确保同一时间只有一个chip显示操作按钮。点击其他区域时通过事件监听器清除激活状态。

